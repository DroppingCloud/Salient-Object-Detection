% -*- coding: utf-8 -*-
%-------------------------designed by zcf--------------
\documentclass[UTF8,a4paper,10pt]{ctexart}
\usepackage[left=3.17cm, right=3.17cm, top=2.74cm, bottom=2.74cm]{geometry}
\usepackage{amsmath}
\usepackage{graphicx}
\usepackage{float}
\usepackage{cite}
\usepackage{bm}
\usepackage{caption}
\usepackage{enumerate}
\usepackage{booktabs} %表格
\usepackage{multirow}
\usepackage{subcaption}    % 插入子图
\usepackage[table]{xcolor}
\usepackage{siunitx}
\usepackage{tcolorbox}
\tcbuselibrary{listings,breakable}
\usepackage{fontspec} % 允许使用 Unicode 字体
\usepackage{mdframed}
\usepackage{tabularx}
\usepackage{textcomp}

\usepackage{minted}
\usepackage{xcolor}
\usepackage{geometry}
\geometry{a4paper, margin=1in}

\usepackage{alltt}


\definecolor{rootcolor}{RGB}{0,70,140}
\definecolor{levelone}{RGB}{220,120,20}
\definecolor{leveltwo}{RGB}{150,70,200}
\definecolor{levelthree}{RGB}{0,150,200}
\definecolor{leafgreen}{RGB}{0,150,70}

\sisetup{
  table-format = +3.3,
  detect-weight = true,
  detect-inline-weight = math,
  round-mode = places,
  round-precision = 3
}
\mdfdefinestyle{topbottom}{
  linewidth=1pt,
  topline=true,
  bottomline=true,
  leftline=false,
  rightline=false,
  skipabove=10pt,
  skipbelow=10pt,
  innertopmargin=6pt,
  innerbottommargin=6pt,
  backgroundcolor=gray!5   % 淡灰背景（可去掉）
}

\newcommand{\tabincell}[2]{\begin{tabular}{@{}#1@{}}#2\end{tabular}}  %表格强制换行
%-------------------------字体设置--------------
% 中文字体
\setCJKmainfont[AutoFakeBold=true]{Kaiti SC}
\setCJKsansfont{Yuanti SC}
\setCJKmonofont{Kaiti SC}
\newCJKfontfamily\headinghei{Hei}
% 英文字体
\setmainfont{Palatino}
\setsansfont{Annai MN}
\setmonofont{Menlo}

\newcommand{\yihao}{\fontsize{26pt}{36pt}\selectfont}           % 一号, 1.4 倍行距
\newcommand{\erhao}{\fontsize{22pt}{28pt}\selectfont}          % 二号, 1.25倍行距
\newcommand{\xiaoer}{\fontsize{18pt}{18pt}\selectfont}          % 小二, 单倍行距
\newcommand{\sanhao}{\fontsize{16pt}{24pt}\selectfont}  %三号字
\newcommand{\xiaosan}{\fontsize{15pt}{22pt}\selectfont}        % 小三, 1.5倍行距
\newcommand{\sihao}{\fontsize{14pt}{21pt}\selectfont}            % 四号, 1.5 倍行距
\newcommand{\banxiaosi}{\fontsize{13pt}{19.5pt}\selectfont}    % 半小四, 1.5倍行距
\newcommand{\xiaosi}{\fontsize{12pt}{18pt}\selectfont}            % 小四, 1.5倍行距
\newcommand{\dawuhao}{\fontsize{11pt}{11pt}\selectfont}       % 大五号, 单倍行距
\newcommand{\wuhao}{\fontsize{10.5pt}{15.75pt}\selectfont}    % 五号, 单倍行距
%-------------------------章节名----------------
\renewcommand\thesubsection{\arabic{subsection}}
\renewcommand\thesubsubsection{\thesubsection.\arabic{subsubsection}}

\ctexset{
  section = {
    name = {,、},
    number = \chinese{section},
    format = \Large\bfseries\centering\headinghei,
  },
  subsection = {
    name = {,、},
    number = \arabic{subsection},
    format = \large\bfseries\headinghei,
    indent = 0em,
    beforeskip = 1.0ex,
    afterskip = 1.0ex,
  },
  subsubsection = {
    name = {,},
    number = \thesubsubsection,
    format = \wuhao\bfseries\headinghei,
    indent = 0em,
    beforeskip = 1.0ex,
    afterskip = 1.0ex,
  }
}

%------------------------图表编号-------------------
\usepackage{chngcntr}

\counterwithin{figure}{section}
\counterwithin{table}{section}
\counterwithin{equation}{section}

%-------------------------页眉页脚--------------
\usepackage{lastpage}
\usepackage{fancyhdr}
\pagestyle{fancy}
\lhead{\kaishu \leftmark}
% \chead{}
\rhead{\kaishu 基于深度学习的显著性目标检测方法研究}%加粗\bfseries
\lfoot{}
\rfoot{}
\renewcommand{\headrulewidth}{0.1pt}
\renewcommand{\footrulewidth}{0pt}%去掉横线
\newcommand{\HRule}{\rule{\linewidth}{0.5mm}}%标题横线
\newcommand{\HRulegrossa}{\rule{\linewidth}{1.2mm}}
%-----------------------伪代码------------------
\usepackage{algorithm}
\usepackage{algorithmicx}
\usepackage{algpseudocode}
\floatname{algorithm}{Algorithm}
\renewcommand{\algorithmicrequire}{\textbf{Input:}}
\renewcommand{\algorithmicensure}{\textbf{Output:}}
\usepackage{lipsum}
\makeatletter
\newenvironment{breakablealgorithm}
  {% \begin{breakablealgorithm}
  \begin{center}
     \refstepcounter{algorithm}% New algorithm
     \hrule height.8pt depth0pt \kern2pt% \@fs@pre for \@fs@ruled
     \renewcommand{\caption}[2][\relax]{% Make a new \caption
      {\raggedright\textbf{\ALG@name~\thealgorithm} ##2\par}%
      \ifx\relax##1\relax % #1 is \relax
         \addcontentsline{loa}{algorithm}{\protect\numberline{\thealgorithm}##2}%
      \else % #1 is not \relax
         \addcontentsline{loa}{algorithm}{\protect\numberline{\thealgorithm}##1}%
      \fi
      \kern2pt\hrule\kern2pt
     }
  }{% \end{breakablealgorithm}
     \kern2pt\hrule\relax% \@fs@post for \@fs@ruled
  \end{center}
  }
\makeatother

\cfoot{\thepage\ of \pageref{LastPage}}%当前页 of 总页数
%------------------------代码-------------------
\usepackage{xcolor}
\usepackage{listings}
\lstset{
breaklines,%自动换行
basicstyle=\small,
keywordstyle=\color{ blue!70} \bfseries,
commentstyle=\color{red!50!green!50!blue!50},%
stringstyle=\ttfamily,%
extendedchars=false,%
linewidth=\textwidth,%
numbers=left,%
numberstyle=\tiny \color{blue!50},%
frame=trbl,%
rulesepcolor= \color{ red!20!green!20!blue!20}
}
\setminted{
  linenos,
  breaklines,
  fontsize=\small,
  frame=lines,
  numbersep=5pt,
  bgcolor=gray!5,
  tabsize=4
}
%------------超链接----------
\usepackage[colorlinks,linkcolor=black,anchorcolor=blue]{hyperref}
%------------------------TODO-------------------
\usepackage{enumitem,amssymb}
\newlist{todolist}{itemize}{2}
\setlist[todolist]{label=$\square$}
% for check symbol
\usepackage{pifont}
\newcommand{\cmark}{\ding{51}}%
\newcommand{\xmark}{\ding{55}}%
\newcommand{\done}{\rlap{$\square$}{\raisebox{2pt}{\large\hspace{1pt}\cmark}}\hspace{-2.5pt}}
\newcommand{\wontfix}{\rlap{$\square$}{\large\hspace{1pt}\xmark}}
%------------------------水印-------------------
\usepackage{tikz}
\usepackage{xcolor}
\usepackage{eso-pic}

\newcommand{\watermark}[3]{\AddToShipoutPictureBG{
\parbox[b][\paperheight]{\paperwidth}{
\vfill%
\centering%
\tikz[remember picture, overlay]%
  \node [rotate = #1, scale = #2] at (current page.center)%
    {\textcolor{gray!80!cyan!30!magenta!30}{#3}};
\vfill}}}

%———————————————————————————————————————————正文———————————————————————————————————————————————
%----------------------------------------------
\begin{document}
\begin{titlepage}
    \begin{center}
    \includegraphics[width=0.8\textwidth]{fig/NKU.png}\\[1cm]
    \textsc{\Huge \kaishu{\textbf{南\ \ \ \ \ \ 开\ \ \ \ \ \ 大\ \ \ \ \ \ 学}} }\\[0.9cm]
    \textsc{\huge \kaishu{\textbf{计\ \ 算\ \ 机\ \ 学\ \ 院}}}\\[0.5cm]
    \textsc{\Large \textbf{《深度学习与应用》期末大作业}}\\[0.8cm]
    \HRule \\[0.9cm]
    { \LARGE \bfseries 基于深度学习的显著性目标检测方法研究}\\[0.4cm]
    \HRule \\[2.0cm]
    \centering
    \kaishu\LARGE
    \renewcommand{\arraystretch}{1.6}
    \setlength{\tabcolsep}{12pt}

    \begin{tabular}{c c c c}
    成员 1：& 陈天祺 & 2313857 & 计算机科学与技术 \\
    成员 2：& 邵莫涵 & 2311366 & 计算机科学与技术 \\
    成员 3：& 杨欣瑞 & 2312246 & 计算机科学与技术 \\
    \end{tabular}
    \vfill
    {\Large \today}
    \end{center}
\end{titlepage}

%-------------目-------录-----------------------------------------
\newpage
\pagenumbering{gobble}
\tableofcontents
\thispagestyle{empty}

%----------------------------------------------------------------
\newpage
\pagenumbering{arabic}
\setcounter{page}{1}

%----------------------------------------------------------------
\section{摘要}

显著目标检测旨在从自然场景图像中定位并分割最具视觉吸引力的前景目标，是图像分割、目标理解、图像编辑和视觉预处理中的重要基础任务。
本文围绕基于深度学习的显著性目标检测方法展开研究，基于 PyTorch 实现并比较了多种典型模型结构，包括 PoolNet、GateNet、$\text{F}^3$ Net 和 CPD 等代表性网络，
并在此基础上进一步探索了深监督、边缘约束、门控筛选、多尺度上下文建模以及跨层特征交互等改进策略。
具体而言，本文以 ResNet-18 和 ResNet-34 \cite{he2016deep} 作为主要骨干网络，构建了 PoolNet-DS、PoolNet-Gate、PoolNet-ASPP、PoolNet-CFM、
PoolNet-ASPP-Gate、PoolNet-ASPP-CFM、PoolNet-Gate-CFM 和 PoolNet-CFM(Enhanced) 等多种扩展模型；
同时也对 GateNet-CBAM、GateNet-DS、$\text{F}^3$ Net-ASPP、$\text{F}^3$ Net-CBAM 以及 CPD 系列改进模型进行了实现和对比。
实验部分采用 ECSSD、DUTS 和 HKU-IS 等公开数据集，从 MaxF、MAE、$S_m$ 和 $F_m^{\beta}$ 等指标对不同模型进行定量评估。
实验结果表明，多层特征融合、跨层一致性建模等方案能够有效提升显著预测图的区域完整性与边界质量，
其中 PoolNet-CFM(Enhanced) 在多个评测基准下取得了最优表现，并在 DUTS-TR 训练后保持了较好的跨数据集泛化能力。我们的项目代码公开发布在 \url{https://gitee.com/clouddropping/salient-object-detection}。

\bigskip
\noindent\textbf{关键词：}显著目标检测；ResNet；跨层特征融合；多尺度上下文

%----------------------------------------------------------------
\section{引言}

显著目标检测（Salient Object Detection, SOD）旨在从输入图像中预测最吸引视觉注意力的目标区域，
通常输出与原图同尺寸的像素级显著图。与一般语义分割任务不同，SOD 并不关心前景目标的语义类别，
而是关注图像中哪些区域在视觉上更应被突出。因此，该任务既需要模型具备全局语义理解能力，
以判断目标与背景之间的整体关系，又需要保留精细的局部结构，以生成边界清晰、区域完整的显著性掩码。
由于显著图可以作为后续视觉任务的前景先验，SOD 被广泛应用于图像检索、图像压缩、图像编辑、目标分割和场景理解等任务中
\cite{borji2014salient,wang2019salient}。

早期显著性检测方法通常依赖颜色、纹理、中心偏置、区域对比度或图结构等手工特征，
通过启发式规则刻画前景目标与背景区域之间的差异。这类方法在目标颜色对比明显、背景结构简单的图像上能够取得一定效果，
但在低对比度、多目标、小目标、复杂纹理背景以及目标边界模糊等场景下往往鲁棒性不足。
随着全卷积网络（Fully Convolutional Network，FCN）\cite{long2015fully}、
U-Net \cite{ronneberger2015unet} 和 ResNet \cite{he2016deep} 等深度网络结构的发展，
基于卷积神经网络的端到端显著性检测方法逐渐成为主流。
深度模型能够从大规模数据中学习多层语义表示，相比传统手工特征方法具有更强的表达能力和泛化能力。

尽管深度 SOD 方法已经取得显著进展，但该任务仍然面临多层特征融合和结构细节恢复方面的困难。
一方面，深层特征具有较强语义判别能力，有利于定位显著目标主体，但其空间分辨率较低，容易损失边界和细节；
另一方面，浅层特征保留了丰富的纹理与边缘信息，却也包含大量背景噪声。
因此，如何在编码器--解码器框架中有效融合不同层级特征，是显著性目标检测模型设计中的核心问题。
现有方法从不同角度进行了探索：PoolNet \cite{liu2019simple} 通过全局引导和池化式特征聚合增强多尺度上下文，
GateNet \cite{zhao2020suppress} 通过门控机制抑制不可靠的低层特征，
$\text{F}^3$ Net \cite{wei2020f3net} 通过跨层特征交互与反馈解码缓解直接相加或拼接带来的语义不一致问题，
CPD \cite{wu2019cascaded} 则利用部分解码结构在精度与效率之间取得平衡。

本文围绕显著性目标检测中的特征融合、注意力筛选和多尺度上下文建模问题展开实验研究。
我们首先复现并比较 PoolNet、GateNet、$\text{F}^3$ Net 和 CPD 等典型模型，形成统一的训练、验证和评估流程，
随后从各基线模型出发，设计了多种结构变体，包括加入深监督与边缘分支的 PoolNet-DS、
引入空间与通道双分支门控的 PoolNet-Gate、增强深层上下文表达的 PoolNet-ASPP、
使用跨层乘法交互的 PoolNet-CFM，以及进一步组合 Gate、ASPP \cite{chen2017rethinking}、CFM 和前景门控的增强模型。
此外，本文还对 GateNet、$\text{F}^3$ Net 和 CPD 系列进行了 CBAM \cite{woo2018cbam}、ASPP、
深监督、门控与细节提示等方向的扩展，从多个角度分析不同模块对显著性检测性能的影响。

本文主要工作总结如下：

\begin{enumerate}
  \item 搭建了统一的显著性目标检测实验框架，复现了 PoolNet、GateNet、$\text{F}^3$ Net、CPD 等多类代表性模型，并支持基于 ResNet-18 与 ResNet-34 骨干网络的公平比较。
  \item 围绕 PoolNet 设计了多组改进模型，系统研究深监督、边缘约束、双分支门控、ASPP 多尺度上下文、CFM 跨层交互以及前景门控对检测性能的影响。
  \item 对 GateNet、$\text{F}^3$ Net 和 CPD 系列进一步加入 CBAM、ASPP、深监督、门控和细节增强等模块，形成跨模型系列的结构对比。
  \item 在 ECSSD、DUTS 和 HKU-IS 数据集上进行定量评估和消融分析，验证不同模块在区域完整性、背景抑制、边界保持和跨数据集泛化方面的作用。
\end{enumerate}

%----------------------------------------------------------------
\section{相关工作}

% ----------------------------------------------------------------
\subsection{基于 CNN 的显著性检测方法}

显著性目标检测早期主要依赖人工设计的低层视觉线索，例如颜色对比、亮度差异、纹理一致性、空间紧凑性以及中心先验等。
这类方法通常从局部或全局对比度出发构造显著性线索，再通过图模型、区域排序或后处理方法生成显著图。
相关综述指出，传统方法在简单场景下具有一定解释性，但其表达能力受限于手工特征设计，
难以稳定处理复杂背景、相似前景背景、多目标以及弱边界等情况 \cite{borji2014salient}。

深度学习的发展显著推动了 SOD 方法的性能提升。FCN \cite{long2015fully} 将分类网络改造为端到端的像素级预测模型，
为密集预测任务提供了基本范式；U-Net \cite{ronneberger2015unet} 通过编码器--解码器结构和跳跃连接将高层语义与低层空间信息结合，
对后续显著性检测网络产生了重要影响。基于 ResNet \cite{he2016deep} 等强骨干网络的 SOD 模型进一步提高了特征表达能力，
使模型能够从多层卷积特征中同时获取目标语义、空间位置和局部边界信息。

% ----------------------------------------------------------------
\subsection{编码器--解码器结构与多层特征融合}

显著性目标检测需要兼顾全局定位和局部细节，编码器--解码器结构是深度 SOD 中最常用的网络范式。
编码器通常由 ImageNet 预训练的分类网络构成，用于提取从低层纹理到高层语义的多尺度特征；
解码器则通过上采样、跳跃连接和特征聚合逐步恢复空间分辨率，并生成最终显著图。
然而，不同层级特征在语义层次、感受野和空间分辨率上存在明显差异，
如果仅使用简单相加或拼接进行融合，可能导致高层语义与浅层细节之间响应不一致，
从而引入背景噪声或削弱目标边界。

PoolNet \cite{liu2019simple} 是一种具有代表性的池化式显著性检测网络。
该方法通过全局引导模块为不同解码层提供显著目标的高层位置信息，
并利用特征聚合模块在自顶向下路径中逐步融合多尺度上下文。
本文以 PoolNet 作为主要实验基线，在其基础上设计了 PoolNet-DS、PoolNet-Gate、PoolNet-ASPP、PoolNet-CFM、
PoolNet-ASPP-Gate、PoolNet-ASPP-CFM、PoolNet-Gate-CFM 和 PoolNet-CFM(Enhanced) 等变体，
用于分析监督方式、门控筛选、多尺度感受野和跨层交互对解码质量的影响。

$\text{F}^3$ Net \cite{wei2020f3net} 针对普通特征融合忽略层间差异的问题，
提出了 Cross Feature Module（CFM）和 Cascaded Feedback Decoder（CFD）。
其中 CFM 通过跨层特征乘法交互选择互补信息，避免将无关响应直接传递到解码器；
CFD 则通过反馈机制逐步精炼特征表示。
本文的 PoolNet-CFM、PoolNet-Gate-CFM、PoolNet-ASPP-CFM 和 PoolNet-CFM(Enhanced)
借鉴了 CFM 的跨层一致性思想，将其移植到 PoolNet 的解码节点中，
以缓解高层语义特征与浅层空间特征直接融合时的语义偏差。

CPD \cite{wu2019cascaded} 从效率角度重新审视多层特征聚合，
认为低层大分辨率特征虽然包含细节信息，但计算成本较高且对最终性能的贡献并不总是稳定。
因此，CPD 采用部分解码器优先聚合高层特征，并利用生成的显著图反向增强主干特征表示。
本文实现了 CPD 基线及其 ASPP、CBAM、CFM、Gate、深监督、整体注意力和细节提示等扩展版本，
用于与 PoolNet 和 GateNet 系列进行横向比较。

% ----------------------------------------------------------------
\subsection{注意力机制、门控筛选与多尺度上下文}

注意力机制和门控机制常用于抑制显著性检测中的背景干扰。
CBAM \cite{woo2018cbam} 通过串联的通道注意力和空间注意力对中间特征进行自适应重标定，
能够突出更有判别性的语义通道和空间区域。GateNet \cite{zhao2020suppress} 则在编码器与解码器之间引入门控单元，
分别控制 FPN 分支和并行分支中特征的传递强度，从而在抑制冗余背景响应的同时保留局部细节。
受此启发，本文在 PoolNet-Gate 中设计了通道门控与空间门控并行的 Dual-Gate 结构，
先对浅层 skip 特征进行筛选，再送入解码融合过程。同时在 GateNet-CBAM、GateNet-DS 以及 CPD-Gate 等模型中
进一步考察注意力或门控机制在不同网络框架下的适用性。

多尺度上下文建模是显著性检测中的另一类关键技术。
自然图像中的显著目标在尺度、形态和数量上差异较大，单一感受野难以同时兼顾小目标细节和大目标整体结构。
ASPP \cite{chen2017rethinking} 通过多个不同 dilation rate 的空洞卷积分支并行提取上下文信息，
已被广泛用于语义分割等密集预测任务。
本文在 PoolNet-ASPP、PoolNet-ASPP-Gate、PoolNet-ASPP-CFM、$\text{F}^3$ Net-ASPP 和 CPD-ASPP 中引入 ASPP 思想，
重点考察深层解码阶段增强感受野后对显著目标定位和尺度鲁棒性的影响。

此外，深监督与边缘约束也常用于改善显著图的训练稳定性和边界质量。
本文在 PoolNet-DS、GateNet-DS、CPD-DS 等模型方案中设置辅助预测分支，
使中间解码特征能够直接接收监督信号，并且在 PoolNet-DS 中进一步使用由真实掩码梯度生成的边缘监督，
引导浅层高分辨率特征关注目标轮廓。

%----------------------------------------------------------------
\section{Baseline 方法}

%----------------------------------------------------------------
\subsection{ResNet}

%----------------------------------------------------------------
\subsubsection{基本原理}

ResNet\cite{he2016deep} 是深度卷积神经网络中的经典骨干结构，其核心思想是通过残差连接缓解深层网络训练中的退化问题。
与传统卷积网络直接学习目标映射 $H(x)$ 不同，残差网络学习的是输入与输出之间的残差部分 $F(x)=H(x)-x$。这种设计使得网络在加深时更
容易学习恒等映射，从而降低深层网络的优化难度。

残差块（Residual Block）是 ResNet 的基本组成单元（图 \ref{fig:residual_block}），主要由残差分支和捷径分支两部分构成。残差分支用于对输入特征进行非线性变换，捷径分支则将输入特征直接传递到输出端，并与残差分支的输出进行逐元素相加。

\begin{figure}[H]
  \centering
  \includegraphics[width=0.7\textwidth]{fig/resnet1.png}
  \caption{Residual block}
  \label{fig:residual_block}
\end{figure}

%----------------------------------------------------------------
\subsubsection{网络结构}

ResNet 通常由一个初始卷积层和若干个残差阶段组成（图~\ref{fig:resnet}）。以 ImageNet 版 ResNet 为例，
输入图像先经过 7×7 conv + BN + ReLU + maxpool 得到低层特征，然后依次经历 layer1、layer2、layer3、layer4 四个残差阶段。

每个阶段由多个残差块组成，空间分辨率逐步下降，通道数逐步增加。一般情况下，ResNet18/34 使用 BasicBlock，ResNet50/101 使用更深的 Bottleneck Block。

\begin{figure}[t]
  \centering
  \includegraphics[width=0.7\textwidth]{fig/resnet3.png}
  \caption{ResNet 网络结构}
  \label{fig:resnet}
\end{figure}

%----------------------------------------------------------------
\subsubsection{基于 ResNet 的显著目标检测网络}

显著目标检测（Salient Object Detection, SOD）旨在从图像中预测最具视觉吸引力的前景区域。由于该任务需要生成像素级显著图，因此网络既需要高层语义信息来定位显著目标，又需要低层空间信息来恢复目标边界。

基于 ResNet 的显著目标检测网络通常采用编码器-解码器（Encoder-Decoder）结构。其中 ResNet 作为编码器提取多尺度特征，
解码器负责融合不同层级特征并生成显著图。

\begin{align*}
    S = D(C_1, C_2, C_3, C_4)
\end{align*}

其中 $C1, C2, C3, C4$ 表示解码器输出的各层级特征，$D(\cdot)$ 表示解码器，$S$ 表示输出显著图。

% ----------------------------------------------------------------
\subsection{PoolNet}

% ----------------------------------------------------------------
\subsubsection{基本思想}

PoolNet \cite{liu2019simple}是一种基于编码器—解码器结构的显著目标检测网络（图~\ref{fig:poolnet}），其核心思想是充分融合深层语义信息与浅层空间细节信息，从而生成区域完整、边界清晰的显著性预测图。
PoolNet 的核心创新点包括：

\begin{itemize}
    \item \textbf{全局上下文增强}：通过顶层特征的多尺度池化，使网络感受野扩大，可区分局部高对比度背景与真正显著目标。
    \item \textbf{多尺度特征融合}：通过自顶向下的解码过程将高层语义信息与浅层空间信息融合，增强浅层特征的表达能力并抑制噪声。
\end{itemize}

\begin{figure}[t]
  \centering
  \includegraphics[width=1.0\textwidth]{fig/poolnet.png}
  \caption{PoolNet 模型架构}
  \label{fig:poolnet}
\end{figure}

% ----------------------------------------------------------------
\subsubsection{金字塔池化模块（PPM）}

PoolNet 引入金字塔池化模块（Pyramid Pooling Module，PPM）用于捕获多尺度上下文信息。
对于深层输入特征 \( X_{\text{deep}} \in \mathbb{R}^{B \times C \times H/32 \times W/32} \) ，首先经过多尺度自适应平均池化形成多尺度全局上下文特征：
\[
X_{s} = \text{Upsample}\Big(\text{AvgPool}_{s}(X_{\text{deep}})\Big)
\]
其中 \(s \in \{1,3,5\}\) 表示不同池化输出尺寸，Upsample 将池化特征上采样回深层特征分辨率。然后再与原始深层特征进行拼接增强：
\[
X_{\text{PPM}} = \text{Conv}\Big([X_{\text{deep}}, X_{1}, X_{3}, X_{5}]\Big)
\]

PPM 输出随后上采样到浅层特征尺寸，用作 Global Guiding Flow (GGF)，为解码器提供全局语义指导。

% ----------------------------------------------------------------
\subsubsection{深度池化特征融合（Deep Pooling Layer）}

在解码阶段，PoolNet 采用自顶向下融合的策略，核心模块是深度池化层（Deep Pooling Layer）。其处理流程为：

\begin{enumerate}
    \item 高层特征 \(F_{\text{high}}\) 上采样至当前层特征分辨率，
    并与当前层特征 \(F_{\text{skip}}\) 及 GGF 全局信号特征 \(F_{\text{info}}\) 相加。
    \[
    F_{\text{merge}} = F_{\text{high}}^{\uparrow} + F_{\text{skip}} + F_{\text{info}}
    \]
    
    \item 对融合后的特征执行多尺度平均池化（Adaptive Average Pooling），最后每个池化分支通过卷积和 ReLU 后上采样回原始分辨率，
    融合不同尺度的上下文信息，从而增强感受野。
    \[
    F_{\text{pool}} = F_{\text{merge}} + \sum_{s \in \{2,4,8\}} \text{Upsample}\Big(\text{AvgPool}_{s}(F_{\text{merge}})\Big)
    \]
    
    \item 对融合后的特征进行卷积整合。
    \[
    F_{\text{out}} = F_{3\times3}(F_{\text{pool}})
    \]
\end{enumerate}

这种自顶向下的融合策略保证了浅层特征同时拥有高分辨率空间信息和深层语义信息，
提升显著图对边缘和细节的敏感性，同时抑制浅层噪声干扰。

% ----------------------------------------------------------------
\subsection{GateNet}

% ----------------------------------------------------------------
\subsubsection{基本思想}

GateNet \cite{zhao2020suppress} 是一种基于门控机制的显著目标检测网络（图 \ref{fig:gatenet}），
核心思想是在编码器与解码器之间引入门控单元，自适应控制不同层级特征向解码器传递的信息量。
GateNet 的核心创新点包括：

\begin{itemize}
    \item \textbf{特征筛选}：通过门控权重抑制非显著区域响应，减少背景干扰。
    \item \textbf{层级平衡}：根据不同编码层对预测结果的贡献，动态调整特征融合强度。
    \item \textbf{双分支互补}：FPN 分支负责主体区域预测，Parallel 分支补充边界和细节信息。
    \item \textbf{多尺度增强}：利用 Fold-ASPP 增强深层特征的上下文表达能力。
\end{itemize}

\begin{figure}[t]
  \centering
  \includegraphics[width=1.0\textwidth]{fig/gatenet.png}
  \caption{GateNet 模型架构}
  \label{fig:gatenet}
\end{figure}

% ----------------------------------------------------------------
\subsubsection{Fold-ASPP}

对于深层语义特征，GateNet 引入 Fold-ASPP 模块以增强多尺度上下文建模能力。普通 ASPP 通过不同空洞率的卷积扩大感受野，但较大的空洞率会导致采样点之间相关性减弱，不利于细节结构建模。
而 Fold-ASPP 通过 Fold 操作将局部邻域折叠到通道维度，使空洞卷积作用于局部区域而非孤立采样点，从而在扩大感受野的同时保留局部结构连续性（图~\ref{fig:fold}）。

Fold-ASPP 采用多条并行分支提取不同尺度的上下文信息，多尺度分支输出融合后，为后续解码器提供更强的高级语义指导。

\begin{figure}[t]
  \centering
  \includegraphics[width=0.7\textwidth]{fig/fold-aspp.png}
  \caption{普通空洞卷积与折叠空洞卷积对比}
  \label{fig:fold}
\end{figure}

% ----------------------------------------------------------------
\subsubsection{Gate Units}

传统 FPN 或 U-Net 通常直接将编码器特征传递给解码器，但这种 all-pass skip connection 可能将背景噪声和误导性上下文一并引入解码过程。
为此，GateNet 设置门控单元（Gate Units）显式控制编码器特征向解码器传递的信息量。

对于第 \(i\) 层，门控单元同时接收当前编码器特征 \(E_i\) 和上一层解码器特征 \(D_{i+1}\) 来生成门控权重：
\[
G_i =
P\Big(
\sigma(
\text{Conv}([E_i, D_{i+1}])
)
\Big)
\]
其中 \([\,\cdot\,]\) 表示通道拼接，\(\sigma(\cdot)\) 为 Sigmoid 函数，\(P(\cdot)\) 表示全局平均池化。

对于最高层，由于不存在更高层解码器特征，门控权重由编码器特征和对应的过渡层特征共同生成。
\[
G_i =
P\Big(
\sigma(
\text{Conv}([E_i, T_i])
)
\Big)
\]

门控单元输出两个权重 $G_i = [G_i^{1}, G_i^{2}]$，
其中 \(G_i^{1}\) 作用于 FPN 分支，控制当前层特征参与逐级解码的强度；
\(G_i^{2}\) 作用于 Parallel 分支，用于选择补充残差信息的编码器特征。

% ----------------------------------------------------------------
\subsubsection{FPN 分支}

FPN 分支负责生成主要的显著性预测，其解码过程采用自顶向下融合策略：高层语义特征逐级上采样，并与当前层经过门控筛选的编码器特征融合。

\[
D_i =
\text{Conv}
\Big(
G_i^{1} \cdot T_i + \text{Up}(D_{i+1})
\Big)
\]

在这种机制下，高层特征提供全局语义，低层特征补充空间细节，门控单元则在二者之间起到抑制背景干扰、平衡层级贡献的作用。

% ----------------------------------------------------------------
\subsubsection{Parellel 分支}

逐级解码结构可能导致部分细节信息在自顶向下融合过程中被削弱，GateNet 为此增加了并行分支，将各层经过第二组门控权重筛选后的特征直接汇聚起来，用于补充 FPN 分支遗漏的信息。

\[
F_{\text{cat}} =
\text{Cat}
\Big(
D_1,
\text{Up}(G_1^{2} \cdot T_1),
\text{Up}(G_2^{2} \cdot T_2),
\cdots,
\text{Up}(G_n^{2} \cdot T_n)
\Big)
\]

随后通过卷积生成残差预测，并与 FPN 分支结果相加。
\[
S_{\text{final}}
=
\text{Conv}(F_{\text{cat}}) + D_1
\]

其中 \(D_1\) 表示 FPN 分支得到的初步显著性预测。该残差结构使并行分支不必重新预测完整显著图，而是专注于补充边界、细长结构和局部细节。

从功能上看，FPN 分支更依赖高层语义信息，用于确定显著目标主体；Parellel 分支更强调低层细节信息，用于恢复局部结构。二者通过门控机制形成互补。


% ----------------------------------------------------------------
\subsection{$\text{F}^3$ Net}



% ----------------------------------------------------------------
\subsection{CPD Net}



% ----------------------------------------------------------------
\section{探索与创新}

% ----------------------------------------------------------------
\subsection{PoolNet-DS}

PoolNet-DS 在 PoolNet 主干结构基础上引入多尺度深监督（Deep Supervision）与边缘感知约束。
原始 PoolNet 仅在最终解码特征上生成显著图，训练信号需要经过完整解码路径反向传播到中间层，
容易使中高层融合节点缺少直接监督。为缓解这一问题，我们在三个中间解码阶段分别设置辅助显著性预测头，
并在浅层高分辨率特征上增加边缘预测分支，使网络同时关注区域完整性与边界结构。

设自顶向下解码过程中得到的三个中间特征为 \(M_3,M_2,M_1\)，其中 \(M_1\) 分辨率最高、包含更多局部细节。
PoolNet-DS 对每个中间特征使用独立的多尺度预测头生成辅助显著图：
\[
S_i^{aux}
=
\operatorname{Up}
\left(
H_i(M_i)
\right)
\]
其中 \(H_i(\cdot)\) 表示由两层 \(3\times3\) 卷积和 \(1\times1\) 预测卷积组成的辅助头，
\(\operatorname{Up}(\cdot)\) 表示上采样至输入图像尺寸。
对于浅层特征 \(M_1\)，额外使用边缘分支预测边界图：
\[
E_1
=
\operatorname{Up}
\left(
\operatorname{Conv}_{1\times1}
\left(
\operatorname{ReLU}
\left(
\operatorname{BN}
\left(
\operatorname{Conv}_{3\times3}(M_1)
\right)
\right)
\right)
\right)
\]
训练时，主显著图仍采用 BCE、SSIM 与 Soft IoU 的组合损失；
辅助显著图采用 BCE 与 Soft IoU 约束，边缘分支则以由真实掩码 Sobel 梯度生成的边缘图作为监督信号。
因此，整体损失可写为：
\[
\mathcal{L}
=
\mathcal{L}_{main}
+
\sum_i \lambda_i \mathcal{L}_{aux}(S_i^{aux},G)
+
\lambda_e \mathcal{L}_{edge}(E_1,\nabla G)
\]
其中 \(G\) 表示真实显著性标注，\(\nabla G\) 表示归一化后的 Sobel 边缘图。

% ----------------------------------------------------------------
\subsection{PoolNet-Gate}

PoolNet-Gate 针对 PoolNet 中 skip connection 直接相加可能引入背景噪声的问题，
在每个需要融合浅层特征的 Deep Pooling Layer 中加入双分支门控模块。
该模块并不改变 PoolNet 的整体编码器--解码器框架，而是在高层解码特征与浅层 skip 特征融合之前，
自适应筛选浅层特征中的有效通道和有效空间位置。

具体而言，给定当前解码特征 \(D\) 与同分辨率浅层特征 \(L\)，通道门控分支首先对 \(D\) 做全局平均池化，
再通过两层 \(1\times1\) 卷积生成通道权重：
\[
A_c
=
\sigma
\left(
W_2
\delta
\left(
W_1
\operatorname{GAP}(D)
\right)
\right),
\quad
L_c = A_c \odot L
\]
其中 \(\sigma(\cdot)\) 为 Sigmoid 函数，\(\delta(\cdot)\) 为 ReLU 激活，\(\odot\) 表示逐元素乘法。
空间门控分支则拼接 \(D\) 与 \(L\)，通过卷积预测空间注意力图：
\[
A_s
=
\sigma
\left(
\operatorname{Conv}_{1\times1}
\left(
\delta
\left(
\operatorname{BN}
\left(
\operatorname{Conv}_{3\times3}([D,L])
\right)
\right)
\right)
\right),
\quad
L_s = A_s \odot L
\]
两路门控结果进一步拼接并用卷积融合：
\[
\hat{L}
=
\operatorname{Conv}_{3\times3}([L_c,L_s])
\]
最终，PoolNet-Gate 的融合形式由原始的
\(D+L+I\) 改为 \(D+\hat{L}+I\)，其中 \(I\) 为 PPM 产生的全局引导特征。
这种设计使通道门控负责选择语义相关的特征维度，空间门控负责抑制非显著区域响应，
二者互补地提升了浅层细节特征的可靠性。

% ----------------------------------------------------------------
\subsection{PoolNet-ASPP}

PoolNet-ASPP 的动机是增强深层解码阶段的多尺度上下文建模能力。
原始 PoolNet 虽然通过 PPM 提供全局语义引导，并在 Deep Pooling Layer 内使用多尺度平均池化聚合上下文，
但在最深层解码时仍主要依赖局部卷积完成特征变换。
显著目标检测中，目标尺度变化较大，复杂背景中的局部高响应区域也容易被误判为显著目标；
因此，我们在最深的两个解码单元中用 ASPP 模块替换原有 FAM 结构，以增加不同感受野下的语义判别能力。

设深层输入特征为 \(X\)，ASPP 模块包含 \(1\times1\) 卷积分支、多个空洞卷积分支以及全局平均池化分支：
\[
Y
=
\operatorname{Proj}
\left(
\left[
\operatorname{Conv}_{1\times1}(X),
\operatorname{Conv}_{3\times3}^{r=2}(X),
\operatorname{Conv}_{3\times3}^{r=4}(X),
\operatorname{Conv}_{3\times3}^{r=8}(X),
\operatorname{Up}(\operatorname{GAP}(X))
\right]
\right)
\]
其中 \(r\) 表示空洞卷积的 dilation rate，\(\operatorname{Proj}(\cdot)\) 为 \(1\times1\) 投影卷积。
在解码流程中，前两个从深到浅的融合节点采用
\[
D_i
=
\operatorname{ASPP}
\left(
\operatorname{Up}(D_{i+1})
\right)
+
L_i
+
I_i
\]
而浅层两个节点仍保留 PoolNet 原有的 FAM 结构。
这样的分层设计使深层特征更关注尺度鲁棒的语义定位，浅层特征则继续负责边界和局部结构恢复，
避免在所有层统一使用 ASPP 带来的计算冗余。

% ----------------------------------------------------------------
\subsection{PoolNet-CFM}

PoolNet-CFM 将原始 PoolNet 中的直接加法融合替换为跨层特征交互模块（Cross Feature Module，CFM）。
直接相加默认高层解码特征与浅层 skip 特征已经在语义上对齐，
但二者分别偏向语义定位与空间细节，简单相加可能导致响应混杂。
因此，PoolNet-CFM 借鉴 \(\text{F}^3\) Net 中的交互思想，
先分别对两路特征进行卷积变换，再通过乘法建模二者的一致响应。

给定解码特征 \(D\) 和浅层特征 \(L\)，CFM 首先得到两路二阶特征：
\[
D_2 = f_d^{(2)}(f_d^{(1)}(D)),
\quad
L_2 = f_l^{(2)}(f_l^{(1)}(L))
\]
随后使用逐元素乘法获得交互特征：
\[
F_{\times} = D_2 \odot L_2
\]
该乘法项相当于软一致性约束，也就是只有在高层语义与浅层细节共同响应的位置，
融合特征才会被显著增强。之后，交互结果分别回注到两条分支并进行残差精炼：
\[
\hat{D}
=
f_d^{(4)}
\left(
f_d^{(3)}(F_{\times}) + f_d^{(1)}(D)
\right),
\quad
\hat{L}
=
f_l^{(4)}
\left(
f_l^{(3)}(F_{\times}) + f_l^{(1)}(L)
\right)
\]
最终输出为
\[
F_{cfm} = \hat{D} + \hat{L}
\]
在 PoolNet-CFM 的每个融合节点中，解码特征先经过 \(3\times3\) 卷积调整通道，
再与 skip 特征送入 CFM，最后叠加全局引导特征 \(I\) 并进入 FAM：
\[
D_i
=
\operatorname{FAM}
\left(
\operatorname{CFM}(D_{i+1}^{\uparrow},L_i)+I_i
\right)
\]
该结构使融合过程从无条件相加升级为语义与细节一致性驱动的交互融合。

% ----------------------------------------------------------------
\subsection{PoolNet-Gate-CFM}

PoolNet-Gate-CFM 进一步将门控筛选与 CFM 交互融合结合起来。
PoolNet-Gate 可以在融合前抑制浅层背景噪声，PoolNet-CFM 则能够增强高层语义与浅层细节的一致响应，
可以形成互补。

在该模型中，每个需要 skip 融合的解码单元首先利用双分支门控得到净化后的浅层特征：
\[
\hat{L}_i
=
\operatorname{DualGate}(D_i,L_i)
\]
随后将当前解码特征与 \(\hat{L}_i\) 输入 CFM：
\[
D_i'
=
\operatorname{CFM}(D_i,\hat{L}_i)+I_i
\]
最后再通过 FAM 进行多尺度池化增强：
\[
D_i
=
\operatorname{FAM}(D_i')
\]
其中，门控模块减少 skip 特征中与显著目标无关的通道和空间响应，
CFM 则在较干净的浅层特征基础上建模跨层乘法关系。

% ----------------------------------------------------------------
\subsection{PoolNet-ASPP-Gate}

PoolNet-ASPP-Gate 采用分层混合解码策略：在深层两个解码单元使用 ASPP，
在浅层两个解码单元使用双分支 Gate。
这一设计基于显著目标检测中不同层级特征的功能差异，
深层特征分辨率较低但语义信息强，更适合通过 ASPP 扩大感受野、建模尺度变化；
而浅层特征分辨率较高但背景噪声较多，更适合通过门控机制筛选边界与局部纹理信息。

令 \(i=0,1\) 表示较深的两个解码阶段，\(i=2,3\) 表示较浅的两个解码阶段，
PoolNet-ASPP-Gate 的解码单元可概括为：
\[
D_i =
\begin{cases}
\operatorname{ASPP}(D_{i+1}^{\uparrow}) + L_i + I_i, & i \in \{0,1\}\\
\operatorname{FAM}(D_{i+1}^{\uparrow} + \operatorname{DualGate}(D_{i+1}^{\uparrow},L_i) + I_i), & i = 2\\
\operatorname{FAM}(D_{i+1}), & i = 3
\end{cases}
\]
相比于所有层统一引入同一种模块，这种方式更符合 PoolNet 自顶向下解码的语义分工。
其中深层负责确定显著目标的大致位置与尺度，浅层负责恢复边缘和细节。

% ----------------------------------------------------------------
\subsection{PoolNet-ASPP-CFM}

PoolNet-ASPP-CFM 关注深层语义增强与跨层交互融合的联合建模，
在最深两个解码阶段同时使用 ASPP 与 CFM。
ASPP 先增强当前解码特征的多尺度语义表达，CFM 再将其与对应 skip 特征进行乘法交互。

对于深层融合节点，其计算过程为：
\[
\tilde{D}_i
=
\operatorname{ASPP}(D_{i+1}^{\uparrow})
\]
\[
D_i
=
\operatorname{CFM}(\tilde{D}_i,L_i)+I_i
\]
浅层节点则保留原始 PoolNet 的 FAM 结构：
\[
D_i
=
\operatorname{FAM}(D_{i+1}^{\uparrow}+L_i+I_i)
\]
这种设计的核心假设是，深层语义特征经过 ASPP 之后具有更强的尺度鲁棒性，
再与 skip 特征进行乘法交互时，可以更准确地选择与显著目标相关的空间细节。

% ----------------------------------------------------------------
\subsection{PoolNet-CFM(Enhanced)}

PoolNet-CFM(Enhanced) 是在 PoolNet-CFM 基础上得到的增强版本。
该模型保留 CFM 作为主要跨层融合方式，同时对解码尾部加入前景门控（Foreground Gate），
用于根据深层融合结果对最终高分辨率特征进行再加权。

具体而言，模型首先执行与 PoolNet-CFM 相同的自顶向下解码，并在第一个融合节点得到深层引导特征 \(G_f\)：
\[
G_f = D_0
\]
随后经过后续解码单元得到最终特征 \(D_{final}\)。
前景门控以 \(G_f\) 为 guide 预测单通道前景权重图：
\[
A_f
=
\sigma
\left(
\operatorname{Conv}_{1\times1}
\left(
\delta
\left(
\operatorname{BN}
\left(
\operatorname{Conv}_{3\times3}(G_f)
\right)
\right)
\right)
\right)
\]
最终特征按如下方式进行门控：
\[
\hat{D}_{final}
=
D_{final}
\odot
\left(
(1-\alpha)+\alpha A_f
\right)
\]

从模型设计上看，Enhanced 版本将 CFM 的跨层一致性建模与深层前景先验结合起来，
既利用浅层细节恢复边界，又利用深层语义约束最终响应区域。

%----------------------------------------------------------------
\section{实验}

%----------------------------------------------------------------
\subsection{数据集}

% 本文采用 DUTS-TR \cite{wang2017duts} 作为训练集，并选取 DUTS-TE \cite{wang2017duts}、DUT-OMRON \cite{yang2013dut-omron}、HKU-IS \cite{li2015hkuis}、ECSSD \cite{yan2013ecssd} 和 PASCAL-S \cite{li2014pascal-s} 五个公开数据集作为测试集，以全面评估模型在显著性目标检测任务中的性能。上述数据集均属于静态图像显著性目标检测数据集，每张图像均对应一张像素级显著性真值图。

% DUTS 数据集是显著性目标检测领域中规模较大、使用较广泛的数据集之一，包含训练集 DUTS-TR 和测试集 DUTS-TE 两个部分。其中，DUTS-TR 包含 10,553 张自然场景图像及其对应的像素级显著性标注，本文将其作为模型训练数据，用于学习图像中显著目标的区域分布、边界结构和前景背景差异。DUTS-TE 则作为测试集，用于评价模型在同源数据分布下的检测性能。

% 除 DUTS-TE 外，本文还进一步在 DUT-OMRON、HKU-IS、ECSSD 和 PASCAL-S 上进行测试，以考察模型的跨数据集泛化能力。其中，DUT-OMRON 背景复杂、目标尺度和数量变化较大，能够检验模型在复杂场景下的鲁棒性；HKU-IS 包含较多高质量自然图像，适合评价模型对常规显著目标的检测能力；ECSSD 图像结构和语义信息较为复杂，对模型的边界保持和结构完整性提出了较高要求；PASCAL-S 来源于 PASCAL VOC，类别丰富、语义关系复杂，常用于检验模型在复杂语义场景下的泛化能力。

% \begin{table}[t]
% \centering
% \caption{Statistics of Datasets}
% \label{tab:sod_datasets}
% \begin{tabular}{clcccc}
% \hline
% \textbf{No.} & \textbf{Dataset} & \textbf{Split} & \textbf{Total Images} & \textbf{GT Images}\\
% \hline
% 1 & DUTS-TR    & Train & 10,553 & 10,553\\
% 2 & DUTS-TE    & Test  & 5,019  & 5,019 \\
% 3 & DUT-OMRON  & Test  & 5,168  & 5,168 \\
% 4 & HKU-IS     & Test  & 4,447  & 4,447 \\
% 5 & ECSSD      & Test  & 1,000  & 1,000 \\
% 6 & PASCAL-S   & Test  & 850    & 850   \\
% \hline
% \end{tabular}
% \end{table}

本实验采用 ECSSD 数据集 \cite{yan2013ecssd}、DUTS 数据集 \cite{wang2017duts} 和 HKU-IS 数据集 \cite{li2015hkuis} 进行模型训练与测试（表~\ref{tab:dataset}），
以验证模型在不同数据规模和不同场景复杂度下的有效性。其中，ECSSD 用于小规模数据条件下的快速验证，DUTS-TR 用于大规模训练，HKU-IS 则用于进一步评估模型在自然图像场景下的泛化性能。

ECSSD 是显著性目标检测领域常用的基准数据集之一，包含 1000 张自然场景图像及其对应的像素级显著性标注图。该数据集场景复杂、目标类别丰富，常见前景背景对比不明显、目标边界复杂、目标尺度变化较大以及背景干扰较强等情况，
因此能够较好地反映自然场景下显著目标检测任务的实际难度。

DUTS 是显著性目标检测任务中常用的大规模数据集，包含训练集 DUTS-TR 和测试集 DUTS-TE 两部分。其中，DUTS-TR 包含 10553 张图像，DUTS-TE 包含 5019 张图像，数据规模明显大于 ECSSD。
DUTS 数据集具有场景类型丰富、目标形态多样、样本数量充足等特点，适合作为显著性目标检测模型的训练数据。

HKU-IS 是显著性目标检测领域常用的测试数据集，包含 4447 张自然图像及其像素级显著性标注。相比 ECSSD，HKU-IS 数据规模更大，图像质量较高，包含较多自然场景中的显著目标，适合用于评估模型在常规自然图像场景下的检测稳定性和跨数据集泛化能力。

基于上述数据集，本实验设置了两种验证方式：一是\textbf{按照 7:3 划分比例在 ECSSD 数据集上进行训练与测试}，用于快速验证模型在小规模数据条件下的性能；二是\textbf{采用 DUTS-TR 进行训练，并在 ECSSD 和 HKU-IS 数据集上进行测试}，用于评估模型在较大规模训练数据支持下的迁移能力与泛化性能。

\begin{table}[H]
\centering
\caption{显著目标检测数据集概览}
\label{tab:dataset}
\setlength{\tabcolsep}{6pt}
\renewcommand{\arraystretch}{1.12}

\begin{tabular}{c|c|c|c|l}
\specialrule{1.2pt}{0pt}{0pt}

\textbf{No.} & \textbf{Dataset} & \textbf{Split} & \textbf{Images} & \textbf{Description} \\
\specialrule{1.0pt}{0pt}{0pt}

1 & ECSSD   & Train/Test & 1000  & 小规模基准数据集，场景复杂、边界细节丰富 \\
2 & DUTS-TR & Train      & 10553 & 大规模训练集，场景丰富、目标形态多样 \\
3 & DUTS-TE & Test       & 5019  & DUTS 测试集，用于评价同源分布下的检测性能 \\
4 & HKU-IS  & Test       & 4447  & 高质量自然图像数据集，用于评估泛化能力 \\

\specialrule{1.2pt}{0pt}{0pt}
\end{tabular}
\end{table}

\begin{figure}[t]
  \centering

  \begin{subfigure}[t]{0.43\textwidth}
    \centering
    \includegraphics[height=0.22\textheight]{fig/ecssd.png}
    \caption{ECSSD}
    \label{fig:ecssd_samples}
  \end{subfigure}
  \hspace{0.04\textwidth}
  \begin{subfigure}[t]{0.43\textwidth}
    \centering
    \includegraphics[height=0.22\textheight]{fig/duts.png}
    \caption{DUTS}
    \label{fig:duts_samples}
  \end{subfigure}

  \caption{ECSSD 和 DUTS 数据集样例图}
  \label{fig:dataset_samples}
\end{figure}

%----------------------------------------------------------------
\subsection{评价指标}

% %----------------------------------------------------------------
% \subsubsection{Precision-Recall(PR)}

% Precision-Recall 曲线用于衡量模型在不同阈值下的检测性能。其中，Precision 表示模型预测
% 为显著目标的像素中实际为显著目标的比例，反映预测结果的准确性；Recall 表示真实显著目标像素
% 中被模型正确检测出的比例，反映模型对目标区域的覆盖能力。二者定义如下：
% \begin{align*}
%   Precision&=\frac{|M \cap G|}{|M|}\\
%   Recall&=\frac{|M \cap G|}{|G|}\\
% \end{align*}

% 其中，$M$ 表示预测显著图二值化结果，$G$ 表示真实标注图。在实际评价中，通过改变二值化阈值，
% 可以得到多组 Precision 和 Recall 值，并绘制 Precision-Recall 曲线。
% 通常情况下，PR 曲线越接近右上角，说明模型在保持较高召回率的同时仍能维持较高精确率，检测效果越好。

%----------------------------------------------------------------
\subsubsection{MAE}

MAE（Mean Absolute Error）表示平均绝对误差，用于衡量预测显著图与真实标注图之间的像素级差异。其定义为：
\begin{equation*}
  MAE=\frac{1}{W\times H}\sum_{x=1}^W\sum_{y=1}^H|S(x,y)-G(x,y)|
\end{equation*}

其中，$S$ 表示预测显著图，$G$ 表示真实标注图。MAE 直接在连续显著图上进行计算，
不依赖于二值化阈值。因此，MAE 能够反映预测图与真实标注图在整体像素层面的接近程
度。MAE 数值越小，说明模型生成的显著图与真实标注越接近，预测误差越低。

%----------------------------------------------------------------
\subsubsection{F-measure}

F-measure 是 Precision 和 Recall 的加权调和平均，用于综合衡量模型的准确性和完整性。其定义为：
\begin{equation*}
  F_{\beta}=\frac{(1+\beta^2)\cdot Precision \cdot Recall}{\beta^2\cdot Precision + Recall}
\end{equation*}

其中，$\beta$ 用于调节 Precision 和 Recall 的相对重要性，通常设置 $\beta^2=0.3$。
F-measure 数值越大，说明模型在显著目标区域检测的准确性和完整性方面表现越好。

%----------------------------------------------------------------
\subsubsection{Weighted F-measure}

Weighted F-measure（$F_{\beta}^{w}$）是传统 F-score 的加权扩展形式，用于更加准确地评价显著性检测结果。普通 F-score 主要依据像素级 Precision 和 Recall 计算模型性能，但对预测误差的空间位置和结构影响考虑不足。为此，$F_{\beta}^{w}$ 引入加权机制，对不同区域的预测误差赋予不同权重，从而能够更好地反映显著目标的区域完整性、边界质量以及背景抑制效果。
其定义为：
\begin{equation*}
  F_{\beta}^{w}
  =
  \frac{(1+\beta^2)\cdot Precision^{w}\cdot Recall^{w}}
  {\beta^2\cdot Precision^{w}+Recall^{w}}
\end{equation*}

其中，$Precision^{w}$ 表示加权准确率，$Recall^{w}$ 表示加权召回率，$\beta$ 用于调节二者的相对重要性。$F_{\beta}^{w}$ 数值越大，说明预测显著图与真实标注图在像素分布和空间结构上越一致，模型的显著性检测性能越好。

% %----------------------------------------------------------------
% \subsubsection{E-measure}

% E-measure（Enhanced-alignment Measure）是一种综合考虑全局统计信息与局部像素匹配关系的显著性检测评价指标，用于衡量预测显著图与真实标注图之间的整体对齐程度和结构一致性。其定义为：
% \begin{equation*}
%   E_{\phi}=\frac{1}{W\times H}\sum_{x=1}^{W}\sum_{y=1}^{H}\phi(x,y)
% \end{equation*}

% 其中，$\phi(x,y)$ 表示增强对齐矩阵在像素位置 $(x,y)$ 处的取值，$W$ 和 $H$ 分别表示图像的宽度和高度。E-measure 在评价过程中不仅关注预测结果与真实标注之间的像素级差异，还引入了全局均值信息来刻画二者在整体区域分布上的一致性。因此，相比仅基于像素分类结果的评价指标，E-measure 能够更全面地反映显著目标区域的完整性、前景与背景的区分能力以及预测结果的结构质量。
% E-measure 越大表示预测显著图与真实标注图之间的一致性越高，模型的显著性检测效果越好。

%----------------------------------------------------------------
\subsubsection{S-measure}

S-measure（Structural-measure，$S_m$）是一种用于评价显著性检测模型预测结果与真实标注之间结构一致性的指标。S-measure 同时考虑区域一致性（Region-aware）与对象一致性（Object-aware）两方面信息，
从而能够更全面地评估预测显著图在整体形状、前景分布以及边界结构上的质量。其定义为：
\begin{equation*}
  S_m = \alpha \cdot S_o + (1-\alpha) \cdot S_r
\end{equation*}

其中，$S_o$ 表示对象级一致性（Object-aware similarity），度量预测显著图与真实标注在前景整体分布上的相似程度；$S_r$ 表示区域级一致性（Region-aware similarity），衡量局部区域的结构和边界对齐效果；$\alpha$ 是调节二者相对重要性的平衡系数，通常取 0.5。
S-measure 越大，说明预测显著图在结构和形状上越接近真实标注，能够更好地反映显著目标的完整性、边界精确度以及前景与背景的分离效果。


%----------------------------------------------------------------
\subsection{实验细节}

本项目基于 PyTorch 深度学习框架实现，使用 NVIDIA RTX 4090 GPU 完成模型训练与测试。
为保证不同模型之间的公平比较，各实验均采用 ImageNet 预训练的 ResNet18 或 ResNet-34 作为骨干网络。
实验默认使用 DUTS-TR 作为训练集，并在 ECSSD 数据集上进行测试与性能评估。

在数据预处理阶段，图像被缩放为 $320 \times 320$ 作为网络输入。
训练阶段采用随机水平翻转进行数据增强，同时使用 ImageNet 预训练模型的统计量对输入图像进行归一化。
显著性标注图的二值化阈值设置为 $0.5$。

优化器采用 AdamW，权重衰减系数设为 $5\times10^{-4}$。
为避免预训练骨干网络参数发生过大扰动，实验对骨干网络与新建层采用不同学习率：
骨干网络学习率设为 $3\times10^{-5}$，其余新建参数学习率设为 $2\times10^{-4}$。
所有模型均训练 25 个 epoch。学习率调度采用 MultiStep 策略，
在第 15 个和第 22 个 epoch 将当前学习率衰减为原来的 $\frac{1}{10}$。

训练损失由显著性检测中常用的像素级分类损失与结构约束损失共同组成。
对于主输出，采用 BCE、SSIM Loss 与 Soft IoU Loss 的加权组合：
\[
\mathcal{L}_{main}
=
\mathcal{L}_{BCE}
+
\mathcal{L}_{SSIM}
+
\left(1-\operatorname{IoU}_{soft}\right)
\]
其中 Soft IoU 定义为
\[
\operatorname{IoU}_{soft}
=
\frac{\sum_i p_i g_i}
{\sum_i (p_i + g_i - p_i g_i) + \epsilon}
\]
$p_i$ 表示预测显著图在第 $i$ 个像素处的概率值，$g_i$ 表示对应的真实标注值，
$\epsilon$ 为防止除零的极小常数。


% 对于具有多尺度输出或深监督分支的模型，除主输出损失外，还对辅助输出施加 BCE 与 Soft IoU 约束：
% \[
% \mathcal{L}_{aux}
% =
% \mathcal{L}_{BCE}
% +
% \left(1-\operatorname{IoU}_{soft}\right)
% \]
% 最终训练目标写作：
% \[
% \mathcal{L}
% =
% \mathcal{L}_{main}
% +
% \lambda \mathcal{L}_{aux}
% \]
% 其中辅助损失权重设为 $\lambda=0.4$。对于仅包含单一输出的模型，则仅计算主输出损失。

%----------------------------------------------------------------
\subsection{实验结果}

为验证不同模型结构在显著目标检测任务中的有效性，我们在 ECSSD 和 HKU-IS 数据集上对多种基线模型及改进模型进行了定量比较，
评价指标包括最大 F-measure（MaxF）、平均绝对误差（MAE）、结构度量（$S_m$）以及加权 F-measure（$F_m^{\beta}$）。
结果如表~\ref{tab:ecssd_hkuis_comparison} 所示。

从实验结果可以看出，在相同训练设置下，ResNet34 骨干网络相比 ResNet18 整体取得了更优的性能，
说明更强的特征表达能力有助于提升显著目标检测效果。各类改进模型中相较于基线模型均取得了一定的提升效果，
基于 PoolNet 的 CFM 增强结构表现最为突出。特别是 PoolNet-CFM(Enhanced) 在 ECSSD 数据集上的各项指标均达到最优，表明所引入的增强模块能够有效改善显著区域的定位精度和结构一致性。

此外，我们对最优模型 PoolNet-CFM(Enhanced) 额外在 DUTS-TR 数据集上进行训练，并在 ECSSD 和 HKU-IS 数据集上验证 Scaling up 
的效果。可以看到，该模型在 ECSSD 和 HKU-IS 两个测试集上均保持了较好的性能，说明该方法在数据规模扩大时仍能保持良好的性能增益，也具备一定的跨数据集泛化能力。

\begin{table}[t]
\centering
\caption{不同方法在各显著目标数据集上的定量比较结果。\textcolor{blue}{蓝色}表示次优结果，\textcolor{red}{红色}表示最优结果。
\textcolor{gray}{灰色}标注行是最优模型在 DUTS-TR 数据集上训练后的评测结果。}
\label{tab:ecssd_hkuis_comparison}
\setlength{\tabcolsep}{4.5pt}
\renewcommand{\arraystretch}{1.12}
\resizebox{\textwidth}{!}{ % 横向压缩，防止超页宽
\begin{tabular}{l|c|cccc|cccc}
\specialrule{1.2pt}{0pt}{0pt} % 顶部加粗线
\multirow{2}{*}{\textbf{Model}}
& \multicolumn{1}{c|}{\textbf{Training}}
& \multicolumn{4}{c|}{\textbf{ECSSD}}
& \multicolumn{4}{c}{\textbf{HKU-IS}} \\
\cmidrule(lr){2-2} \cmidrule(lr){3-6} \cmidrule(lr){7-10}
& \textbf{Dataset}
& \textbf{MaxF $\uparrow$}
& \textbf{MAE $\downarrow$}
& \textbf{$\bm{S}_m \uparrow$}
& \textbf{$\bm{F}_m^{\bm{\beta}} \uparrow$}
& \textbf{MaxF $\uparrow$}
& \textbf{MAE $\downarrow$}
& \textbf{$\bm{S}_m \uparrow$}
& \textbf{$\bm{F}_m^{\bm{\beta}} \uparrow$} \\
\specialrule{1.0pt}{0pt}{0pt} % 加粗中间线

\multicolumn{10}{l}{\textbf{ResNet18 backbone}} \\
\specialrule{1.0pt}{0pt}{0pt} % 加粗小分隔线
PoolNet                 & ECSSD & 0.891 & 0.053 & 0.879 & 0.852 & -- & -- & -- & -- \\
GateNet                 & ECSSD & 0.899 & 0.052 & 0.885 & 0.857 & -- & -- & -- & -- \\
$\text{F}^3$ Net        & ECSSD & 0.885 & 0.057 & 0.875 & 0.841 & -- & -- & -- & -- \\
CPD                     & ECSSD & 0.896 & 0.052 & 0.884 & 0.861 & -- & -- & -- & -- \\
\specialrule{1.0pt}{0pt}{0pt}

\multicolumn{10}{l}{\textbf{ResNet34 backbone}} \\
\specialrule{1.0pt}{0pt}{0pt}
PoolNet                 & ECSSD & 0.909 & 0.045 & 0.897 & 0.875 & -- & -- & -- & -- \\
$\text{F}^3$ Net        & ECSSD & 0.911 & 0.045 & 0.898 & 0.878 & -- & -- & -- & -- \\
GateNet                 & ECSSD & 0.913 & 0.044 & 0.899 & 0.876 & -- & -- & -- & -- \\
$\text{F}^3$ Net-ASPP   & ECSSD & 0.914 & 0.044 & 0.898 & 0.878 & -- & -- & -- & -- \\
$\text{F}^3$ Net-CBAM   & ECSSD & 0.914 & 0.043 & 0.899 & 0.877 & -- & -- & -- & -- \\
CPD                     & ECSSD & 0.915 & 0.043 & 0.902 & 0.884 & -- & -- & -- & -- \\
PoolNet-DS              & ECSSD & 0.912 & 0.042 & 0.901 & 0.881 & -- & -- & -- & -- \\
PoolNet-ASPP            & ECSSD & 0.914 & 0.042 & 0.902 & 0.884 & -- & -- & -- & -- \\
PoolNet-Gate            & ECSSD & 0.915 & 0.040 & 0.905 & 0.888 & -- & -- & -- & -- \\
GateNet-CBAM            & ECSSD & \textcolor{blue}{0.921} & 0.042 & 0.905 & 0.887 & -- & -- & -- & -- \\
GateNet-DS              & ECSSD & 0.920 & 0.041 & 0.905 & 0.886 & -- & -- & -- & -- \\
PoolNet-ASPP-CFM        & ECSSD & 0.919 & 0.041 & 0.906 & 0.889 & -- & -- & -- & -- \\
PoolNet-ASPP-Gate       & ECSSD & 0.917 & \textcolor{blue}{0.039} & 0.907 & 0.889 & -- & -- & -- & -- \\
PoolNet-Gate-CFM        & ECSSD & 0.920 & \textcolor{blue}{0.039} & \textcolor{blue}{0.908} & 0.889 & -- & -- & -- & -- \\
PoolNet-CFM             & ECSSD & 0.918 & \textcolor{blue}{0.039} & \textcolor{blue}{0.908} & \textcolor{blue}{0.891} & -- & -- & -- & -- \\
PoolNet-CFM(Enhanced)   & ECSSD & \textcolor{red}{0.924} & \textcolor{red}{0.037} & \textcolor{red}{0.911} & \textcolor{red}{0.895} & -- & -- & -- & -- \\
\specialrule{0.6pt}{0pt}{0pt}
\rowcolor{gray!20}
PoolNet-CFM(Enhanced)   & DUTS & 0.931 & 0.036 & 0.916 & 0.906 & 0.925 & 0.029 & 0.915 & 0.899 \\
\specialrule{1.2pt}{0pt}{0pt} % 底部加粗线
\end{tabular}
}
\end{table}

另外，我们选取部分模型在给定的图像上进行对比测试，结果如图~\ref{fig:multi-model}。

\begin{figure}[H]
  \centering
  \includegraphics[width=1.0\textwidth]{fig/multi-model.png}
  \caption{不同模型方法的显著预测图对比}
  \label{fig:multi-model}
\end{figure}

为了进一步分析验证模块功能，我们对 PoolNetCFM 模型解码过程中的特征融合变化进行可视化展示。
图~\ref{fig:feats-fusion} 按照由深到浅的三个解码阶段展示了 CFM 融合前后的特征响应变化。
每一行对应一个解码节点，其中 Decoder 表示当前自顶向下传播得到的解码特征，Skip 表示来自编码器对应层级的
横向连接特征，After CFM 表示两者经过跨层特征交互后的输出，CFM Change 表示 CFM 对特征响应带来的变化区域，
After FAM 则表示后续多尺度池化聚合后的结果。

从可视化结果可以看出，在较深的 Decoder 1 阶段，解码特征已经能够较稳定地响应人物主体的大致位置，
但其空间分布仍较为粗糙；对应的 Skip 特征包含更多局部结构信息，同时也引入了墙面纹理、地面边界等背景响应。
经过 CFM 融合后，人物上半身和腿部区域的响应更加集中，而部分与目标无关的背景纹理被削弱，
说明 CFM 的乘法交互能够在高层语义和浅层细节之间形成一致性筛选。随着解码进入 Decoder 2 和 Decoder 3，
Skip 特征中的边缘和纹理信息更加丰富，背景噪声也更加明显；此时 After CFM 和 CFM Change 中目标轮廓、人体边界以及细长结构的响应更突出，
表明 CFM 通过深浅层特征一致性激活，有效保留了与高层显著目标语义一致的空间信息。

此外，After FAM 相比 After CFM 在整体响应上更加平滑，显著目标主体区域的连通性进一步增强。
这说明 CFM 主要负责跨层语义与细节的选择性交互，而后续 FAM 则通过多尺度上下文聚合改善局部响应的连续性。
可见，PoolNet-CFM 的性能提升来源于两个互补过程，
一方面 CFM 抑制浅层 skip 特征中的无关背景响应并恢复空间细节，另一方面多尺度聚合模块进一步整合目标区域上下文，
从而有助于生成结构更完整、背景干扰更少的显著图。

\begin{figure}[t]
  \centering
  \includegraphics[width=1.0\textwidth]{fig/cfm_fusion_panel.png}
  \caption{PoolNet-CFM 解码过程中的跨层特征融合可视化}
  \label{fig:feats-fusion}
\end{figure}


%----------------------------------------------------------------
\subsection{消融实验}

为验证改进方法中各模块的有效性，我们针对部分模型方案进行消融实验，其中 PoolNet-Gate 的
消融实验结果如表~\ref{tab:ablation} 所示。

\begin{table}[H]
\centering
\caption{PoolNet-Gate 各模块在 ECSSD 数据集上的消融实验结果。各列最佳结果使用\textcolor{red}{红色}标出。}
\label{tab:ablation}
\setlength{\tabcolsep}{5pt}
\renewcommand{\arraystretch}{1.15}
\resizebox{\textwidth}{!}{
\begin{tabular}{l|cc|cccc}
\toprule
\multirow{2}{*}{\textbf{Variant}} 
& \multicolumn{2}{c|}{\textbf{Components}} 
& \multicolumn{4}{c}{\textbf{ECSSD}} \\
\cmidrule(lr){2-3} \cmidrule(lr){4-7}
& \textbf{Spatial Gate} 
& \textbf{Channel Gate} 
& \textbf{MaxF $\uparrow$} 
& \textbf{MAE $\downarrow$} 
& \textbf{$\bm{S}_m\uparrow$}
& \textbf{$\bm{F}_m^{\bm{\beta}}\uparrow$} \\
\midrule
\textbf{Baseline} 
&  &  
& 0.909 & 0.045 & 0.897 & 0.875 \\

\textbf{Spatial-only} 
& $\checkmark$ &  
& 0.911 & 0.044 & 0.897 & 0.878 \\

\textbf{Channel-only} 
&  & $\checkmark$ 
& 0.905 & 0.045 & 0.894 & 0.872 \\

\textbf{Dual-gate} 
& $\checkmark$ & $\checkmark$ 
& \textcolor{red}{0.915} & \textcolor{red}{0.040} & \textcolor{red}{0.905} & \textcolor{red}{0.888} \\
\bottomrule
\end{tabular}}
\end{table}

从表~\ref{tab:ablation} 可以看出，单独引入空间门控后，模型的 MaxF 由 0.909 提升至 0.911，
$F_m^{\beta}$ 由 0.875 提升至 0.878，MAE 也略有下降，说明空间维度的显著性筛选能够帮助模型在浅层
skip 特征中抑制部分背景响应，使预测区域更加集中。相比之下，仅使用通道门控时各项指标没有提升，
MaxF、$S_m$ 和 $F_m^{\beta}$ 反而略低于基线模型。这表明在 PoolNet 的解码结构中，单纯依赖全局通道权重
可能会削弱部分有用的局部边界信息，尤其是在 ECSSD 这类目标轮廓和背景结构较复杂的数据集上，
过强的通道重标定并不一定带来稳定收益。

当空间门控与通道门控同时使用时，模型取得了最优结果，MaxF 提升至 0.915，MAE 降低至 0.040，
$S_m$ 和 $F_m^{\beta}$ 分别提升至 0.905 和 0.888。该结果说明两类门控机制具有一定互补性，
空间门控更关注显著目标所在位置，通道门控则对不同语义响应进行选择，二者结合后能够在保留有效边界细节的同时
减少无关背景特征的干扰，改善浅层特征融合的质量。

%----------------------------------------------------------------
\section{成员分工}

本团队队长是陈天祺（2313857），成员分别是杨欣瑞（2312246）和邵莫涵（2311366）。在整个研究过程中，
团队通过飞书、Gitee 等协作平台进行资源共享和信息交流，统一进行资源整合与代码维护。其中，Gitee 的 commit 情况
如图~\ref{fig:gitee}，团队成员分工如表~\ref{tab:division}。


\begin{table}[H]
  \centering
  \caption{成员分工说明}
  \label{tab:division}
  \setlength{\tabcolsep}{5pt}
  \renewcommand{\arraystretch}{1.15}
  \begin{tabularx}{\textwidth}{
    >{\centering\arraybackslash}p{0.12\textwidth}
    >{\raggedright\arraybackslash}p{0.28\textwidth}
    >{\raggedright\arraybackslash}X
  }
    \toprule
    \textbf{成员} & \textbf{主要负责内容} & \textbf{具体工作} \\
    \midrule
    陈天祺 & 训练套件与评测框架搭建、PoolNet 与 GateNet 等基线模型实现与改进、全模型统一评测、文献调研与报告撰写、团队进度协调与仓库代码维护 &
    深入探索调研选题方向，明确相关任务要求和基准数据集与指标；搭建完整的训练工具和评测框架，封装可直接调用的工具方法与可直接执行的快速脚本，引入 DDP 支持分布式训练；
    复现 PoolNet、GateNet 基线模型，另外实现超过 10 种融合与创新优化策略；对所有模型及其扩展版本进行全量统一评测，统计相关测试结果，并进行可视化展示与分析；撰写实验报告超 85\% 内容；
    协调推进团队协作进度，全阶段维护仓库代码和文档。\\

    \midrule
    杨欣瑞 & $\text{F}^3$ Net 系列模型设计、GateNet 相关改进、汇报 PPT 制作 &
    复现 $\text{F}^3$ Net 基线模型，实现 $\text{F}^3$ Net 系列模型改进方案，尝试 GateNet 相关优化；
    制作汇报展示 PPT，撰写实验报告相关部分内容。\\

    \midrule
    邵莫涵 & CPD 系列模型设计 &
    复现 CPD 基线模型，实现 CPD 系列模型改进方案，撰写实验报告相关部分内容。 \\
    \bottomrule
  \end{tabularx}
\end{table}

\begin{figure}[H]
  \centering
  \includegraphics[width=1.0\textwidth]{fig/gitee.png}
  \caption{Gitee 仓库提交记录}
  \label{fig:gitee}
\end{figure}

%----------------------------------------------------------------
\section{总结与展望}


%================================================================
\label{LastMainPage}
\newpage
\bibliographystyle{plain}
\bibliography{references}

\end{document}
